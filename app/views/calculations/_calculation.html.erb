<tr id="<%= dom_id calculation %>">
	<td><%= calculation.id %></td>
	<td><%= calculation.equation %></td>
	<td><%= calculation.result %></td>
	<td><%= time_ago_in_words calculation.created_at %> ago</td>
	<td><%= button_to calculation, confirm: "Sure you want to delete?", method: :delete, remote: true, class: "btn btn-danger" do %>
					<i class="glyphicon glyphicon-trash"></i>
			<% end %>
	</td>
</tr>