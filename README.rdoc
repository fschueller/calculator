== Playground

This is a simple calculator, polished with Twitter Bootstrap, solely for playground purposes.

It currently runs on:

* Rails 4.2.1

* Bootstrap + Sass

* Rspec for tests

and uses SQLite for its secret test deployment.
